# Frontend

此目錄由前端成員主要負責。

- `public/`：不經打包處理的靜態資源。
- `src/components/`：可重複使用的畫面元件。
- `src/pages/`：頁面或路由層級元件。
- `src/services/`：後端 API 呼叫；請依照 `docs/api-contract.md` 實作。
- `src/styles/`：共用樣式與設計 token。
- `tests/`：前端測試。

選定前端框架後，可以直接在此目錄初始化專案，但不要把後端或假資料產製程式放進來。


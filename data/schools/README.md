# School fixture data

此目錄由德國學校假資料成員主要負責。

- 每個學校／學程使用一個獨立 JSON 檔，檔名格式為 `<School Name>-<Programme Name>.json`。
- 例如：`Rheinland Institute of Technology-MSc Data Systems and Artificial Intelligence.json`。
- 所有內容必須是虛構或可公開使用的測試資料。
- 欄位格式以 `docs/data-contract.md` 為準。
- 修改欄位名稱或型別前，先同步更新共同契約並通知前後端成員。

